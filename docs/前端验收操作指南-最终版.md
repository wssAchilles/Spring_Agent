# qKnow 前端验收操作指南（基于实际代码）

> 本指南基于前后端实际代码编写，每项验证均标注了对应的源文件和行号。
> 所有 API 端点均经过代码级确认，非记忆推测。

---

## 一、工具管理页面

### 1.1 工具来源列

**前端代码**：`frontend/src/views/kb/tool/index.vue`
- 第 225 行：`{ key: 6, label: "来源", visible: true }`（列配置）
- 第 81-87 行：`el-table-column` 渲染 `scope.row.source`，三种标签：
  - `source === 'mcp'` → 蓝色 `MCP` 标签
  - `source === 'custom'` → 绿色 `自定义` 标签
  - 其他 → 灰色 `内置` 标签

**后端代码**：`backend/qknow-module-kb/.../controller/admin/tool/KbToolController.java`
- 第 47 行：`@GetMapping("/list")` → `GET /kb/tool/list`
- 返回 `KbToolRespVO`，包含 `source` 字段

**操作步骤**：
1. 登录 → 知识库 → 工具管理
2. 查看表格第 3 列「来源」

**预期结果**：显示 MCP/自定义/内置 标签

**验证命令**：
```bash
grep "key: 6" frontend/src/views/kb/tool/index.vue
grep "source === 'mcp'" frontend/src/views/kb/tool/index.vue
```

---

### 1.2 MCP Server 状态面板

**前端代码**：`frontend/src/views/kb/tool/components/McpServerPanel.vue`
- 第 50 行：`const res = await request({ url: '/kb/health', method: 'get' });`
- 第 51 行：`servers.value = res.data?.mcp?.details || [];`
- 第 59 行：`onMounted(refresh);`（自动加载）

**后端代码**：`backend/qknow-module-kb/.../controller/admin/HealthCheckController.java`
- 第 29 行：`@GetMapping` → `GET /kb/health`
- 第 61-87 行：`checkMcp()` 返回 `{servers, tools, details: [{name, url, command, enabled, connected}]}`

**操作步骤**：
1. 工具管理页顶部查看「MCP Server 状态」卡片
2. 点击刷新按钮

**预期结果**：显示 MCP Server 列表 + 连接状态指示灯

**验证命令**：
```bash
TOKEN=$(curl -s -X POST "http://localhost:8099/login" -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json; print(json.load(sys.stdin).get('token',''))")
curl -s "http://localhost:8099/kb/health" -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['mcp'])"
```

---

## 二、Agent 调试面板

### 2.1 Agent SSE 流式响应

**前端代码**：
- `frontend/src/api/kb/agent/debug.js` 第 9 行：`fetchEventSource` 连接 `POST /kb/agent/testChatMessages`
- `frontend/src/views/kb/agent/index.vue` 第 547-591 行：`doSendMessage()` 处理 SSE 事件
  - 第 560 行：`const data = JSON.parse(event.data);`
  - 第 563 行：`botContent += messageContent;`（累积文本）

**后端代码**：
- `backend/qknow-module-kb/.../controller/admin/agent/KbAgentConfigController.java` 第 153 行：
  `@PostMapping(value = "/testChatMessages", produces = MediaType.TEXT_EVENT_STREAM_VALUE)`
- `backend/qknow-module-kb/.../service/agent/impl/KbAgentConfigServiceImpl.java` 第 200-349 行：
  `chatMessage()` 方法构建 gRPC 请求并返回 `Flux<KbChatMessageSendRespVO>`

**请求格式**：
```json
{
  "botId": 2,
  "question": "你好",
  "modelConfig": "{\"modelId\":1,\"modelName\":\"deepseek-chat\"}",
  "knowledgeIds": "1"
}
```

**响应格式**（SSE 每行）：
```
data:{"code":200,"data":{"send":{...},"receive":{"type":1,"eventType":null,"content":"你好"}},"msg":"操作成功"}
```

**操作步骤**：
1. Bot 管理 → 选择 Bot → Agent 调试
2. 发送问题

**预期结果**：流式显示 assistant 回复

**验证命令**：
```bash
TOKEN=$(curl -s -X POST "http://localhost:8099/login" -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json; print(json.load(sys.stdin).get('token',''))")
curl -s -X POST "http://localhost:8099/kb/agent/testChatMessages" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"botId":2,"question":"你好","modelConfig":"{\"modelId\":1,\"modelName\":\"deepseek-chat\"}","knowledgeIds":"1"}' \
  --max-time 30 -o /tmp/sse.txt && wc -c /tmp/sse.txt
```

---

### 2.2 memory_recall 事件

**后端代码**：`KbAgentConfigServiceImpl.java` 第 321-333 行
```java
memoryReceive.setEventType("memory_recall");
memoryReceive.setContent("召回 " + sourceRefs.size() + " 条记忆");
```
第 336-339 行：如果 `sourceRefs` 非空，Flux 前置 memory_recall 事件

**前端代码**：`MessageList.vue` 第 92-113 行
- 条件：`v-if="item.type === 'memory_recall'"`
- 渲染：橙色头像 + "召回 N 条记忆" + 每条记忆的 content/similarity/decay

**⚠️ 注意**：`index.vue` 第 563 行只累积 `content` 文本，不区分 `eventType`。memory_recall 事件的文本会作为普通消息显示。

**验证命令**：
```bash
curl -s -X POST "http://localhost:8099/kb/agent/testChatMessages" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"botId":2,"question":"总评方案是什么","modelConfig":"{\"modelId\":1,\"modelName\":\"deepseek-chat\"}","knowledgeIds":"1"}' \
  --max-time 60 -o /tmp/sse.txt && grep "memory_recall" /tmp/sse.txt | head -3
```

---

### 2.3 tool_call 事件

**后端代码**：`HermesGrpcClient.java` 第 108-116 行
```java
message.setEventType("tool_call");
message.setToolName(tool.getToolName());
message.setToolStatus(tool.getStatus());
```

**前端代码**：`MessageList.vue` 第 59-90 行
- 条件：`v-if="item.type === 'tool_call'"`
- 渲染：绿色头像 + 工具名 + 状态标签 + 可展开输入/输出 JSON

**⚠️ 注意**：同 memory_recall，`index.vue` 不区分 eventType，tool_call 文本会作为普通消息显示。

**验证命令**：
```bash
grep "tool_call" /tmp/sse.txt | head -3
```

---

## 三、召回测试面板

### 3.1 查询增强信息

**前端代码**：`frontend/src/views/kmc/knowledgeBase/components/recall.vue`
- 第 125-135 行：蓝色框显示 `debugInfo.queryEnhance`
  - `queryEnhance.strategy`（none/rewrite/multi_query/hyde）
  - `queryEnhance.originalQuery`
  - `queryEnhance.variants`（变体列表）

**后端代码**：`RagRetrievalService.java` 第 108-112 行
```java
queryEnhance.put("strategy", rewriteApplied ? "rewrite" : "none");
queryEnhance.put("originalQuery", query);
queryEnhance.put("variants", List.of());
```

**操作步骤**：
1. 知识库详情 → 召回测试 → 开启「显示调试信息」
2. 输入问题 → 测试

**预期结果**：蓝色框显示查询增强策略

**验证命令**：
```bash
curl -s -X POST "http://localhost:8099/kmc/knowledgeBase/recallDebug" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"id":1,"query":"PDF导出","topK":3}' | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['debugInfo']['queryEnhance'])"
```

---

### 3.2 弱路径排除警告

**前端代码**：`recall.vue` 第 138-145 行
- 黄色框显示 `debugInfo.excludedPaths`
- 每项显示 `p.path` + `p.topScore` + `p.threshold`

**后端代码**：`RagRetrievalService.java` 第 112 行
```java
debugInfo.put("excludedPaths", List.of());
```

**预期结果**：如果 `excludedPaths` 非空，显示黄色警告框

---

### 3.3 上下文预算条

**前端代码**：`recall.vue` 第 169-179 行
- 进度条：`contextBytes / maxContextBytes`
- 颜色：绿色（<60%）、黄色（60-80%）、红色（>80%）

**后端代码**：`RagRetrievalService.java` 第 205-206 行
```java
debugInfo.put("contextBytes", context.getBytes(StandardCharsets.UTF_8).length);
debugInfo.put("maxContextBytes", ragContextBuilder.getMaxContextBytes());
```

**验证命令**：
```bash
curl -s -X POST "http://localhost:8099/kmc/knowledgeBase/recallDebug" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"id":1,"query":"OTA固件升级","topK":3}' | python3 -c "
import sys,json; d=json.load(sys.stdin)['data']['debugInfo']
print(f'contextBytes={d[\"contextBytes\"]}, maxContextBytes={d[\"maxContextBytes\"]}')"
```

---

## 四、工作流构建器

### 4.1 工具 Tab

**前端代码**：`frontend/src/views/kb/bot/build/index.vue`
- 第 1289-1292 行：`addNodeMenuTabs = [{ key: "nodes", label: "节点" }, { key: "tools", label: "工具" }]`
- 第 617 行：`@click.stop="handleAddNodeMenuTabChange(tabItem.key)"`（`.stop` 防止冒泡）
- 第 1404-1429 行：`ensureToolMenuItemsLoaded()` 调用 `GET /kb/tool/list` 加载工具

**操作步骤**：
1. Bot 构建页 → 点击边上的「+」按钮
2. 面板显示「节点」和「工具」两个 Tab
3. 点击「工具」Tab

**预期结果**：显示可用工具列表

**验证命令**：
```bash
grep '"tools"' frontend/src/views/kb/bot/build/index.vue
grep "@click.stop" frontend/src/views/kb/bot/build/index.vue | head -3
```

---

## 五、知识图谱

### 5.1 社区着色

**前端代码**：`frontend/src/views/kg/graph/index.vue`
- 第 8 行：`<el-switch v-model="colorByCommunity" active-text="社区着色" inactive-text="类型着色" />`
- 第 95 行：`const colorByCommunity = ref(false);`
- 第 100-111 行：`COMMUNITY_COLORS` 10 种颜色
- 第 177 行：`color: colorByCommunity.value ? getCommunityColor(node.id) : getNodeColor(node.type)`

**后端代码**：`KgGraphController.java` 第 115-131 行
- `POST /kg/graph/communities/detect` → 返回 501（GDS 未安装）或 200

**操作步骤**：
1. 知识图谱页 → 右上角查看社区着色开关
2. 点击「社区检测」按钮

**预期结果**：GDS 未安装时返回 501 + 明确提示

**验证命令**：
```bash
curl -s -X POST "http://localhost:8099/kg/graph/communities/detect?workspaceId=1001" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; print(json.load(sys.stdin))"
```

---

## 六、可观测页面

### 6.1 LangFuse 状态

**前端代码**：`frontend/src/views/kd/observability/index.vue`
- 第 128-139 行：`checkLangFuseStatus()` 调用 `GET /kb/health`
- 第 130 行：`const langfuse = res.data?.langfuse;`
- 第 133 行：`langfuseEnabled.value = langfuse.enabled === true;`

**后端代码**：`HealthCheckController.java` 第 89-96 行
```java
langfuse.put("enabled", "true".equals(System.getenv("LANGFUSE_ENABLED")));
langfuse.put("baseUrl", System.getenv("LANGFUSE_BASE_URL"));
```

**操作步骤**：
1. 访问 `/kd/observability`

**预期结果**：显示「LangFuse 未启用」+ 配置引导步骤

**验证命令**：
```bash
curl -s "http://localhost:8099/kb/health" -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['langfuse'])"
```

---

## 七、审批组件

### 7.1 审批端点

**前端代码**：`frontend/src/views/kb/agent/components/ApprovalDialog.vue`
- 第 75 行：`POST /hermes/approval/approve`（body: `{flowId, nodeId, requestId}`）
- 第 94 行：`POST /hermes/approval/reject`（body: `{flowId, nodeId, requestId, reason}`）

**后端代码**：`ApprovalProxyController.java`
- 第 25 行：`GET /hermes/approval/pending`
- 第 43 行：`POST /hermes/approval/approve`
- 第 54 行：`POST /hermes/approval/reject`
- 第 66 行：`POST /hermes/approval/dev/create`（测试端点）

**操作步骤**：
1. 先创建测试数据：`POST /hermes/approval/dev/create`
2. 查看待审批列表：`GET /hermes/approval/pending`

**验证命令**：
```bash
# 创建测试数据
curl -s -X POST "http://localhost:8099/hermes/approval/dev/create" -H "Authorization: Bearer $TOKEN"
# 查看待审批
curl -s "http://localhost:8099/hermes/approval/pending" -H "Authorization: Bearer $TOKEN"
```

---

## 八、编辑器依赖清理

**前端代码**：`frontend/package.json`
- 确认无 `codemirror`、`@codemirror/*`、`ace-builds`、`vue3-ace-editor`、`vue-codemirror`
- 确认有 `monaco-editor`

**验证命令**：
```bash
grep -c "codemirror" frontend/package.json  # 应为 0
grep -c "ace-builds" frontend/package.json  # 应为 0
grep -c "monaco-editor" frontend/package.json  # 应为 1
```

---

## 九、后端 API 端点汇总

| 端点 | 方法 | 用途 | 后端文件 |
|------|------|------|---------|
| `/kb/health` | GET | 健康检查（database/mcp/langfuse/system） | `HealthCheckController.java:29` |
| `/kb/tool/list` | GET | 工具列表 | `KbToolController.java:47` |
| `/kb/agent/testChatMessages` | POST (SSE) | Agent 调试 | `KbAgentConfigController.java:153` |
| `/kmc/knowledgeBase/recallDebug` | POST | 召回调试 | `KmcKnowledgeBaseController.java:161` |
| `/hermes/approval/pending` | GET | 待审批列表 | `ApprovalProxyController.java:25` |
| `/hermes/approval/approve` | POST | 审批通过 | `ApprovalProxyController.java:43` |
| `/hermes/approval/reject` | POST | 审批拒绝 | `ApprovalProxyController.java:54` |
| `/hermes/approval/dev/create` | POST | 创建测试审批 | `ApprovalProxyController.java:66` |
| `/kg/graph/data` | GET | 图谱数据 | `KgGraphController.java` |
| `/kg/graph/communities/detect` | POST | 社区检测 | `KgGraphController.java:115` |
| `/kb/flow?botId={id}` | GET | 工作流配置 | `KbFlowController.java` |
| `/kb/agent/byBot/{id}` | GET | Agent 配置 | `KbAgentConfigController.java` |

---

## 十、快速验证脚本

```bash
#!/bin/bash
TOKEN=$(curl -s -X POST "http://localhost:8099/login" -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json; print(json.load(sys.stdin).get('token',''))")

echo "=== 1. 健康检查 ==="
curl -s "http://localhost:8099/kb/health" -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; d=json.load(sys.stdin)['data']; print(f'database={d[\"database\"][\"status\"]}, mcp={d[\"mcp\"][\"servers\"]}, langfuse={d[\"langfuse\"][\"enabled\"]}')"

echo "=== 2. RAG 召回 ==="
curl -s -X POST "http://localhost:8099/kmc/knowledgeBase/recallDebug" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"id":1,"query":"PDF导出","topK":3}' | python3 -c "import sys,json; d=json.load(sys.stdin)['data']; print(f'results={len(d[\"results\"])}, queryEnhance={d[\"debugInfo\"][\"queryEnhance\"][\"strategy\"]}, contextBytes={d[\"debugInfo\"][\"contextBytes\"]}')"

echo "=== 3. 审批端点 ==="
curl -s -X POST "http://localhost:8099/hermes/approval/dev/create" -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; print(f'create: {json.load(sys.stdin)[\"code\"]}')"
curl -s "http://localhost:8099/hermes/approval/pending" -H "Authorization: Bearer $TOKEN" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'pending: {len(d[\"data\"])} items')"

echo "=== 4. Agent SSE ==="
curl -s -X POST "http://localhost:8099/kb/agent/testChatMessages" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"botId":2,"question":"你好","modelConfig":"{\"modelId\":1,\"modelName\":\"deepseek-chat\"}","knowledgeIds":"1"}' \
  --max-time 30 -o /tmp/sse.txt && echo "SSE: $(wc -c < /tmp/sse.txt) bytes, memory_recall=$(grep -c memory_recall /tmp/sse.txt), tool_call=$(grep -c tool_call /tmp/sse.txt)"

echo "=== 5. 残留检查 ==="
echo "size=mini: $(grep -rn 'size=\"mini\"' frontend/src/ --include='*.vue' | wc -l)"
echo "append-to refs: $(grep -rn ':append-to=\"\$refs' frontend/src/ --include='*.vue' | wc -l)"
echo "codemirror: $(grep -c 'codemirror' frontend/package.json)"
```

---

*生成时间：2026-06-28*
*基于实际代码验证，非记忆推测*
