#文件预览
upload:
  #  filePath: /data/upload/2023/03/03
  filePath: D:/

# lucene 索引文件位置
lucene:
  indexPath: indexDir

qknow:
  rag:
    eval:
      threshold: 0.85
      prompt-version: manual-v1
      platform: DeepSeek
      model-name: deepseek-chat
      base-url: ${HERMES_OPENAI_BASE_URL:https://api.deepseek.com/v1}
      api-key: ${HERMES_OPENAI_API_KEY:}
    dynamic-top-k:
      enabled: true
      default-top-k: 10
      min-top-k: 3
      max-top-k: 80
      complex-min-top-k: 12
    semantic-cache:
      ttl: 24h
    graph:
      enabled: false
      max-hops: 2
      top-k: 20
